# 雷电构筑与左轮底层重构待办

- ID: `2026-08-20-lightning-build-backlog`
- Status: `planned`
- Branch: `main` working tree
- Created: `2026-08-20`
- Updated: `2026-08-20`

## 同步提醒

- 下一次读取或同步项目上下文时，主动提醒用户：以下事项尚未设计和实现。
- 推荐顺序：先完成 `DiceRevolverGun` 底层重构，再实现弹幕、开火时、命中时和开火后事件。

## 目标

- 重构 `DiceRevolverGun` 底层，使抽面规则、弹夹状态、骰面覆盖、事件触发和伤害成长通过低耦合模块协作。
- 增加雷电主题弹幕与跨弹幕协同构筑。
- 增加可控制骰面抽取顺序、临时复制骰面配置和受限重复触发的事件。

## 已确认事实

- 用户要求记录本工作流，并在下一次同步上下文时主动提示。
- 目前只记录待办，不开始运行时实现。
- 呼应协同最多触发 `4` 次；链式反应复制后必须清空开火后事件以阻止无限复制。

## 已确认构筑方向

- 以**六发编排与连锁**作为核心骨架，让前一发为后一发铺垫。
- 以**弹幕类型、弹幕 Tag 和元素反应**作为主要内容层，形成类似 Noita 的组合反馈。
- 以**抽面控制、骰面返还、复制和风险收益**作为少量高稀有度构筑，提供类似大巴扎的引擎成长感。
- 基础事件负责“发射什么”；开火时事件负责“如何发射”；命中时事件负责“命中后发生什么”；开火后事件负责“如何改变后续枪膛”。
- 单个词条保持简单，强度主要来自跨骰面、跨时刻和跨弹幕的组合；所有连锁继续受事件预算限制。

## 后续扩展参考池

- 弹幕候选：霰弹、穿针弹、回旋弹、符文地雷、弹跳弹、元素胶囊、棱镜分裂弹。
- 开火时候选：前膛记忆、满膛增压、孤注一掷、标签共鸣、镜像枪口。
- 命中时候选：连锁闪电、元素状态反应、破片飞散、猎物标记、埋入弹头。
- 开火后候选：退壳回收、擦亮枪膛、多米诺扳机、空膛红利、顺子奖励、封印骰面。
- 这些候选只作为灵感池，不视为已批准实施范围。

## 待办内容

### 底层

- `DiceRevolverGun` 底层重构。
- 重构必须保护玩家现有枪械调参、四槽位配置、时间系统和事件预算机制。

### 弹幕类型或抽面规则

- **雷电球**：缓慢飞行、可穿透敌人的雷电元素弹幕。
- **收尾者**：绑定的骰面必定在当前弹夹最后一发才会被抽到。

### 开火时事件

- **电磁共鸣**：发射雷电球时，在一定范围内随机选择其他雷电球并产生链式反应；雷电球之间生成能伤害敌人的闪电链。
- **特斯拉**：本轮弹夹每发射一颗雷电类型弹幕，事件绑定骰面的基础弹幕伤害增加 `5%`；换弹后重置本轮成长。

### 命中时事件

- **呼应协同**：同类弹幕命中敌人时，触发一次事件绑定骰面；最多触发 `4` 次，次数耗尽后该骰面视为已消耗。

### 开火后事件

- **链式反应**：将下一发子弹使用的骰面配置覆盖为当前骰面配置，但复制后的开火后槽位为空，防止无限复制链式反应。

## 设计时必须确认

- `收尾者` 更接近抽面约束而非弹丸物理类型，设计时决定其最终资源类别和装备位置。
- 电磁共鸣的搜索半径、链接数量、每条闪电伤害、同一敌人的结算次数和雷电球销毁规则。
- 特斯拉的 `5%` 使用加法叠层还是乘法叠层，以及成长修改运行时快照还是本轮临时修正器。
- 呼应协同的“同类”按弹幕类型、弹幕 Tag 还是两者共同判定；重复触发是否允许继续触发该骰面的命中事件。
- 链式反应覆盖的是下一次抽中的骰面快照，还是直接改变枪膛内骰面配置；被覆盖骰面的原始消耗归属需要明确。

## 非目标

- 当前只记录待办，不修改运行时代码、资源、Prefab、场景或玩家枪械数值。

## 当前正在进行

- 无

## 已完成

- 已记录用户提出的底层重构和六项构筑内容。
- 已记录六发编排、弹幕反应和弹夹经济三层构筑方向，以及后续扩展参考池。

## 下一步

1. 同步上下文时提醒用户本工作流仍为 `planned`。
2. 为 `DiceRevolverGun` 重构梳理职责边界和兼容迁移方案。
3. 确认上述六项设计边界后再编写实施计划。

## 阻塞

- 尚未完成架构设计与用户审核。

## 尚未完成

- 全部运行时设计、实现、资源和测试。

## 涉及文件

- `.project-context/project/workstreams/2026-08-20-lightning-build-backlog/STATE.md`
- `.project-context/project/workstreams/2026-08-20-lightning-build-backlog/HANDOFF.md`
- `.project-context/project/STATUS.md`

## 验证记录

- [not-run] 运行时代码与 Unity 测试未运行，因为本工作流当前只记录待办。

## 相关资料

- [项目地图](../../PROJECT.md)
- [项目状态](../../STATUS.md)
- [既有弹丸事件管线](../2026-08-18-projectile-definition-event-pipeline/STATE.md)
- [骰面四槽位](../2026-08-19-dice-face-four-slots/STATE.md)
